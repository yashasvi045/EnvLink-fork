# =========================================
# 🌐 ENVLINK BACKEND ENVIRONMENT TEMPLATE
# =========================================
# Copy this file to `.env` and fill in your own values.
# NEVER commit `.env` with real secrets.
# =========================================

# -----------------------------
# 🌍 SERVER CONFIGURATION
# -----------------------------
PORT=3001
NODE_ENV=development

# Comma-separated list of allowed frontend URLs
FRONTEND_ORIGINS=http://localhost:3000,https://envlink-web.onrender.com

# -----------------------------
# 🛰️ NASA API KEYS
# -----------------------------
# NASA POWER (Weather & Solar Data)
NASA_API_KEY=DEMO_KEY

# NASA EarthData credentials (for TEMPO / MERRA / GES DISC datasets)
EARTHDATA_USER=
EARTHDATA_PASS=

# Default GIBS imagery layer for satellite map overlays
NASA_GIBS_LAYER=MODIS_Terra_CorrectedReflectance_TrueColor

# -----------------------------
# ☁️ ENVIRONMENTAL DATA SOURCES
# -----------------------------
# OpenAQ v3 API key (register at https://api.openaq.org)
OPENAQ_API_KEY=

# OpenWeatherMap (fallback weather source)
OPENWEATHER_API_KEY=

# -----------------------------
# 🧠 CHATBOT / AI INTEGRATION
# -----------------------------
# You can use either OpenAI or Gemini (leave the other blank)

# Option 1: OpenAI GPT (for chatbot)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Option 2: Google Gemini (for chatbot)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash

# -----------------------------
# 📈 MACHINE LEARNING INTEGRATION
# -----------------------------
# Local path or URL to trained XGBoost model for AQI prediction
XGBOOST_MODEL_PATH=./models/aqi_model.json

# Optional: endpoint of ML microservice if model is hosted separately
ML_SERVICE_URL=

# -----------------------------
# 🗺️ OTHER CONFIGURATIONS
# -----------------------------
# Optional daily tips or quotes API
DAILY_TIP_SOURCE=https://zenquotes.io/api/random

# JWT secret for authentication (change this in production)
JWT_SECRET=changeme_super_secret_key

# Optional database connection string (e.g. PostgreSQL / MongoDB)
DATABASE_URL=

# -----------------------------
# 🩺 HEALTHCHECK
# -----------------------------
# Render / UptimeRobot can ping this route
PING_PATH=/ping

# =========================================
# ✅ END OF ENVLINK BACKEND ENVIRONMENT TEMPLATE
# =========================================
